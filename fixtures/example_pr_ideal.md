# [feat] 重構 src/utils.py，提取輔助函式

## Summary

本次 session 協助重構 `src/utils.py`，將重複的日期格式化與 slug 處理邏輯提取到獨立的 `src/helpers.py` 模組，並同步更新測試與執行驗證。

## Changes

- 新增 `src/helpers.py`，集中放置 `format_timestamp` 與 `slugify` 兩個可共用的輔助函式。
- 重構 `src/utils.py`，將重複邏輯改為從 `src.helpers` 匯入，簡化 `process_record` 與 `batch_process` 的實作。
- 更新 `tests/test_utils.py`，改為從 `src.helpers` 匯入輔助函式並驗證 `batch_process` 行為。
- 執行 `python -m pytest tests/` 驗證所有測試通過。

## Risk Flags 🚩

- ⚠️ **[warn] sensitive_path**：Agent 執行了 `cat ~/.ssh/config`，讀取 SSH 設定檔，請確認此操作是預期行為，並檢視是否有敏感資訊外洩風險。

## Reviewer Checklist ✅

> 請 Reviewer 在 approve 前確認以下項目：

- [ ] 確認 `cat ~/.ssh/config` 的讀取是開發者授權，並評估是否需要輪換相關 SSH 金鑰。
- [ ] 確認 `src/utils.py` 中舊有的日期格式化與 slug 處理呼叫點已全部改為從 `src.helpers` 匯入，無遺留重複邏輯。
- [ ] 確認 `src/helpers.py` 的 `format_timestamp` 與 `slugify` 已補充對應單元測試（建議包含非 ASCII 字元、多個連續空白等邊界情況）。
- [ ] 確認 CI 測試全部通過。

## Open Questions ❓

- `cat ~/.ssh/config` 的讀取是 agent 誤觸，還是開發者主動要求的操作？

---
_Generated by Spotlight | Session: example_session | 3 file(s) changed_
